{
  "rules": {
    "headerMessages": {
      ".read": true
    },
    "tutorials": {
      ".read": true,
    	".indexOn": ["publishedDate", "slug"]
    },
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
